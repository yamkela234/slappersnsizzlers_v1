[project]
name = "slappersnsizzlers"
version = "1.0.0"
requires-python = ">=3.12"

[tool.vercel]
entrypoint = "slappersnsizzlers_v1.wsgi:application"

[tool.vercel.scripts]
build = "python vercel_build.py"
